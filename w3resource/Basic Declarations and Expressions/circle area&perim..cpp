#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main()
{
	int r=6;
	float per,area,pi=3.14159265;
	per=r*2*pi;
	area=r*r*pi;
	printf("Perimeter of circle is %f\n",per);
	printf("Area of circle is %f\n",area);
	system ("pause");
	return 0;
}

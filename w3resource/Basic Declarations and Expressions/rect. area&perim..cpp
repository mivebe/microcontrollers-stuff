#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main()
{
	int a=7,b=5,area,per;
	per=a*2+b*2;
	area=a*b;
	printf("Perimeter of rectangle is %d\n",per);
	printf("Area of rectangle is %d\n",area);
	system("pause");
	return 0;
}
